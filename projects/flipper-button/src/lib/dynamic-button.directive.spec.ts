import { DynamicButtonDirective } from './dynamic-button.directive';

describe('DynamicButtonDirective', () => {
  it('should create an instance', () => {
    // const directive = new DynamicButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
